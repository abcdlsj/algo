#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);

    int N, K;
    while (cin >> N >> K) {
        vector<int> lens(N);
        for (int i = 0; i < N; i++)
            cin >> lens[i];
    }
}