#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
// TODO

class Solution {
public:
    vector<int> ret;
    vector<int> countSmaller(vector<int>& nums) {
        
        return ret;
    }
};

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);
}