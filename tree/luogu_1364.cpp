#include <bits/stdc++.h>
#include <numeric>
using namespace std;
typedef long long ll;
// https://www.luogu.com.cn/problem/solution/P1364
const int MAXN = 1005;
// TODO
vector<int> tree(MAXN);
vector<int> val(MAXN);
int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);
    
    int n; cin >> n;

    for (int i = 0; i < n; i++) {
        int w, u, v; cin >> w >> u >> v;
        
    }
}