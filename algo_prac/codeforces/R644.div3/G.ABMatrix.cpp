#include <bits/stdc++.h>
using namespace std;

int main() {
    int t;
    cin >> t;
    while (t--) {
        // n * m
        int n, m, a, b;
        cin >> n >> m >> a >> b;

        if (b > n || a > m) {
            cout << "NO" << endl;
            continue;
        }
    }
}